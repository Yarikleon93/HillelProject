/**
 * Returns promise resolved with a provided value
 * @param {*} value
 * @returns Promise<*>
 */
export function createResolved(value) {

}


/**
 * Returns a promise rejected with a provided error
 * @param {*} error
 * @returns Promise<*>
 */
export function createRejected(error) {

}

/**
 * Returns a promise which is resolved when value is truthy and rejected otherwise
 * Use "new Promise".
 * See https://learn.javascript.ru/promise for details
 * @param {boolean} value
 * @returns Promise<void>
 */
export function createResolvedOrRejected(value) {

}

/**
 * Returns a promise which is resolved after provided amount of milliseconds
 * Use "new Promise" and setTimeout.
 * See https://learn.javascript.ru/promise for details
 *
 * @param {number} ms
 * @returns Promise<void>
 */
export function delay(ms) {

}